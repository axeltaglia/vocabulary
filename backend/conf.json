{
  "apiPort": "{{.API_PORT}}",
  "dbConfig": {
    "host": "{{.DB_HOST}}",
    "port": "{{.DB_PORT}}",
    "user": "{{.DB_USER}}",
    "password": "{{.DB_PASSWORD}}",
    "dbName": "{{.DB_NAME}}"
  }
}